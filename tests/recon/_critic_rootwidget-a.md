遗漏清单(侦察员清单之外、且不属于其自述未覆盖四类的新增项;行号均来自 `E:/AI_Tools/other/DanZhu/android/main.py`):

1. **转屏/尺寸变化重排链** / 只有 `ws != _last_win_size` 才进(21207-21217), 内含 `LandLayer.apply_orientation` + `_fit_width` + `_apply_sizes` 三连;这是唯一处理横竖屏/分屏/折叠屏的路径,删掉静默不重排(画面构图错乱但不报错) / 21207-21217 / 布局·静默不变量
2. **装杯期 HUD 压暗块**`_build_hud_dim`/`_sync_hud_dim` / 与板面压暗、跑分黑屏是三套不同常数,压的是 GameArea 之外那几行(横屏时最明显);`_frame` 里它后面"不许再早退"是硬约束 / 13731, 13818, 14014, 21427 / 演出
3. **覆盖层重挂**`GameArea._restack_overlays` / `canvas.clear()` 会连子控件 canvas 一起摘掉;漏调用则换盘面/改尺寸后中奖杯与大字整体消失,而指令看起来完全正常 / 12139 / 静默不变量
4. **宽度预算只绑一次**`_budget_bound` / 不守则 `bind(width)` 每跑一遍再挂一条(实测 observer 21→85→341→1365),转屏时整帧卡 1~4 秒且越玩越糟 / 21031-21032 / 静默不变量
5. **落定 0.5 秒超时兜底** / landing 分支的两条出口之一(另一条是"贴近槽中心且 |vy|<10"),88% 走的就是这条;删掉则球永远不进 landed / 21374-21379 / 状态机
6. **落地回弹的物理细节** / 首帧保底 vy≥LAND_BOUNCE_MIN_VY 且 ×JITTER(21339-21346)、触地音的 vy 分档(21361)、回弹 vy 上限的随机化取下半段(21363-21367);`sm-landing-landed` 只写了"回弹落定"这一状态跳转 / 21339-21367 / 演出·物理
7. **哑火帧数上限兜底** / `advance_misfire(...) or _misfire_frames > MISFIRE_MAX_FRAMES`,以及落回柱塞时的 bounce 音 + `park_ball(reroll=False)`;上限触发那条没人提过 / 21329-21336 / 状态机
8. **彩蛋标记生命周期** / `_easter_egg` 在 `launch()` 里必须重置(20129),否则 reset_balance 强断后上一发的标记会污染下一发 / 20129, 21291 / 静默不变量
9. **彩蛋"不计一局"** / 彩蛋分支把 `plays` 与 `round_plays` 各回退 1;`settle-easter` 只覆盖了 ×2 结算,漏了这条账务口径 / 20153-20156 / 结算
10. **统计写入的 0.2 秒延迟** / `Clock.schedule_once(_refresh_stats, 0.20)` 是躲"落袋那一拍"的字形重排;改成 0 会白挪,挪进 `_reveal_win` 会重演揭晓事故 / 20194 / 静默不变量
11. **结果语音抑制窗**`_result_until` / 窗口期内 `set_bet`/`set_rtp` 的语音被抑制(19799、20023),窗口长度还吃 `win_fx.expected_sec(m)`;彩蛋分支自己绕开它直调 sfx.play(20449) / 20161, 20300 / 玩法·静默不变量
12. **中奖震动六档** / `_vibrate(300/220/150/110/75/45)` 按倍率分档,是 settle 里唯一一档纯触觉反馈 / 20287 / 反馈
13. **大奖节奏分档** / `big = m>=10` 决定 `_land_hold`(0.7 vs 0.3+)与 `_anim_dur`(1.2 vs 0.5) / 20205-20207 / 反馈
14. **轮次结束的两个触发点 + 幂等** / `start_charge`(20073)与 `park_ball`(20369)各有一处,靠 `_round_end_shown` 三处读写(13325/20749/20751)去重 / 20073, 20369, 20747-20751 / 状态机·静默不变量
15. **每轮次数更换即重置** / `_set_max_plays` 换档时清空余额/次数/轮次并重置 `_round_end_shown`,与 `reset_balance` 是两套独立清零逻辑 / 20859-20873 / 玩法
16. **隐藏档返还率全流程** / 长按触发只是入口,后续 `_ask_unlock_rtp`(四选一 + 关闭隐藏 + 防重入闸 `_rtp_popup`)、`_unlock_rtp`(只留当前一个)、`_close_rtp_hidden`(摘全部隐藏按钮、切回 `max(RTP_TIERS)`)、且不持久化 / 19864-19986 / 玩法
17. **`_auto_reset_on_start` 静默重置** / 上轮打满被 kill 时,读档后置位、UI 建好后再补一次 `reset_balance(notify=False)`;两条读档分支(20604-20605, 13354) / 20604-20605, 13354 / 静默不变量
18. **声音不持久化 + 静默切换** / `sound_mode` 只存内存、读档侧显式忽略(20567);`toggle_mute` 两个方向都不出声、立即生效、同时刷字色与底色 / 19742-19768, 20567 / 玩法·静默不变量
19. **键盘路径的两条分支** / 空格按住蓄力(14203 起)、松手冻结力度后 **50ms 去抖** 再 `_space_fire`;ESC/Android 返回键被吞掉防误退(14198) / 14193-14216 / 输入
20. **发射键直绑 on_press/on_release** / 与"记 uid 那条 Window 级保底路"是两条并行通路,`launch()` 本身不校验 uid / 14005-14006 / 输入
21. **蓄力期发射键按力度变色** / `_frame` 每帧写 `COL_FIRE`/`COL_FIRE_HOT`,`_restyle_buttons` 在 charging 时**跳开**它;属于独立于力度条的反馈 / 21223-21225 / 反馈
22. **力度标签的清零** / 非蓄力且 power≤0.01 时清空 `power_lbl.text`(全工程唯一赋值处,赋的还是空串) / 21392 / 反馈
23. **帧率上限设定弹窗** / 滑条选档 → `_set_fps_cap_setting` 立即重申高刷 + 落盘 + toast;入口在跑分菜单第 6 个按钮 / 20629-20646, 20701-20705 / 设置
24. **设定异步落盘**`_cfg_post` / `_save_config` 默认走工作线程,退化为同步写;`on_pause` 里 `_cfg_flush()` 等它落完(否则切后台那刻的余额/次数丢) / 20607-20627, 11222, 22113-22119 / 静默不变量
25. **App 生命周期三回调** / `on_pause` 切后台静音 + flush + 返回 True 保 GL 上下文;`on_resume` 重算帧率上限/重申方向/重进沉浸 + resume_out;`on_stop` 关音效并 unschedule 预热链 / 22108, 22121, 22135 / 生命周期
26. **toast 的去重与自适应** / `center_toast` 已有存活 toast 时直接 return;字号按 GameArea **可见宽** 定,避免 360dp 上左右被切 / 12440-12478 / 演出·静默不变量
27. **落袋音与赢音/语音的拆分映射** / `_play_pocket_sound`(含未中 lose)、`_play_win_voice` 的 7 档 tier 映射与"语音档替换琶音"约定 / 20917-20938 / 音效
28. **哑火/非重掷时的灯清除** / `park_ball` 的 `else: lamps_off()` —— 哑火那条路不重掷盘面但必须熄灯,否则槽上攒出一片红绿点 / 20362 / 静默不变量