# 老版 fx_probe.py 的全部门禁(380 条)，按行号排序。
# 这是重构版演出/UI 层必须逐条对等的验收清单。
82	所有 schedule_* 回调都能收 1 个位置参数(否则真机启动即闪退)
90	分层玻璃图可加载
101	玻璃图画布 1600x920(2x)
106	玻璃前层同尺寸
142	整图回退资产同尺寸
144	整图回退资产 glass_tumbler.png 可加载
151	远侧环补画层同尺寸
153	远侧环补画层 glass_tumbler_over.png 可加载
182	4 档投注色球纹理齐全
191	7 档堆形颗数 = 倍率
196	同 (颗数,种子) 逐球心可复现
287	离线坐标表与现算逐位一致(改了球堆参数必须重跑 bake_pile_data.py)
301	表被清空时回退到程序化生成仍能堆出 100 颗
340	档位清单没有第二份手抄本(必须走 _all_rtp())
346	读档白名单走 `_regular_rtp()` 派生 —— 隐藏档'不保存'由此成为**结构性**事实, 不靠谁记得改那行字面量(那行曾经是第三份手抄清单)
355	球堆缓存不超过上限(原型无上限, 长局会一直涨)
397	7 档 expected_sec 都 >= 真实播放时长(兜底 deadline 的基准, 不能偏低)
399	预计时长比实测高出不超过 15%(高太多会让解锁拖沓)
403	最后一颗球触地后还要弹一会儿(触地->停住 > 0.05s), 两时刻确实分开
415	揭晓落在'杯子装满静止'里, 离淡出起点至少还有 0.30s(玩家来得及看清数字)
420	揭晓(触地+REVEAL_DELAY)晚于最后一颗球的最后一次落地音(用户要的语义)
425	最小档(x2)的停留够长(>=0.50s; x2 占中奖 55%%, 低于可辨差等于没改)
427	七档停留**一致** = HOLD_BASE(2026-09-12 起不再按倍率分档: 装满后由**玩家点击**才退场, HOLD_BASE 只当'最短停留'用)。改回递增 = 把分档加回来了, 那会和点击逻辑打架
431	最高档的尾巴离 FX_MAX_SEC=9s 硬兜底还有充裕余量
450	没收到关闭请求时 _layers() 恒返回装满静止态(1,1,1,0) —— 画面纹丝不动地停着, **不许**按退场曲线自己往前走(那会在等待期里把杯子淡掉)
463	退场四分量全程单调(alpha/k 只降, dy 只升)
465	解锁那一刻道具与压暗都已归零(不留给 canvas.clear() 擦成硬切)
469	解锁前 EXIT_ALPHA_TAIL 处道具已全透明
476	WINDUP 那一刻进场曲线正好收在 (1,1,1,0)(和 win 分支严丝合缝, 不会被截断成瞬移)
479	ENTER_CUP_AT + ENTER_CUP_DUR <= WINDUP(否则进场被 tick() 当场截断)
484	进场起手/退场终态的杯底都在槽区隔板顶(606)之上
486	进场起手杯顶没够到大字 cy
504	七档首球入画都 <= 0.20s(锚点 RAIN_ANCHOR=0.12 + 前移封顶的余量)
528	横速清零后, 落定阶段从任意入槽点出发都不会滑出本槽(弹簧只会拉向槽心)
593	每个中奖局的揭晓回调都会被放出去(不许有局丢账)
595	揭晓回调不会放两次
596	一局收尾时回调必须已被消费(挂账=下一局会被重入保护提前执行)
598	揭晓就发生在 reveal_sec() 的 3 帧内(不是拖到下一局开头)
629	_frame 每帧调 _sync_hud_dim(不调 = 压暗层永远停在 0, 静默失效)
631	_sync_hud_dim 在 tick_draw **之后**(演出是靠 tick_draw 推进的; 放前面就晚一帧, 退场结束那帧 HUD 会比板面多黑一整帧)
645	文字纹理缓存是开着的(`_TEXEX_ON`; 关掉时日志会明说「本版关闭」而不是印 0%)
648	`_texex_bake` 里调了 `refresh()` —— 漏了它 `texture` 恒为 None, 缓存静默永远存不进
650	专用 CoreLabel 的属性是**逐个 getattr 抄标签当前的**, 不是写死一份清单
652	颜色按 Kivy 的 `disabled -> disabled_color` 映射抄(不照做的话灰化态会烘成亮色)
655	缓存只对打了 `_texupd_tag` 的标签生效(普通按钮/弹窗不受影响)
657	命中时直接交纹理并 return —— 第一趟/第二趟/上传三样全跳过
659	未命中时烘一个专用 CoreLabel 并存进缓存
662	交纹理时 `texture` 与 `texture_size` **两个都写**(漏后者会整行文字错位)
664	空文字不进缓存(Kivy 那条路给 texture=None/(0,0), CoreLabel 给的是 1x1 占位图, 混了会出鬼)
669	预热链里加了文字纹理预热, 且**一帧只走一个组合**(挤一帧就是自己造一记长帧)
671	预热有一次性闸门, 不会每帧重跑
679	预热表覆盖了状态栏的固定文案
683	两个标题标签只烘**一个色**(它们的 .color 已恒定, 亮暗走画布染色)
686	`_warm_one` 走完**必须还原**文案与颜色(finally)
696	mute_btn / round_btn / 两个标题标签都打了 tag(不打标就不进缓存)
704	有 `_tint_from`(逐通道算系数)与 `_set_lbl_tint`(写画布那条 Color)
706	`_TINT_DIM` 是 `_tint_from` 算出来的, 不是拍脑袋的常数
709	`_set_controls_enabled` 两态都改走染色
711	⚠️ 拿不到画布 Color 时**退回写 `.color`** —— 绝不静默不染色
713	`_TINT_BRIGHT`/`_TINT_DIM` **排在 `_tint_from` 之后**(它是模块级调用, 放前面会 NameError)
716	预热表里列了状态栏带数字的那两句(`命中 x%d · 结算中` / `中奖! +%d (x%d)`)
718	⚠️ 倍率与投注档是**从代码里派生**的(VALUE_SHAPE / PRESETS), 不是手抄一份
720	`_TEXTEX_PER_LABEL` 调大到 64(状态栏要存 10 固定 + 35 带数字 = 45 项; 留在 12 会自己把自己挤掉, 且不报错)
723	日志头部会印缓存命中率(不然没法判断这层到底有没有生效)
729	有跑分专用的固定种子与固定盘面
731	盘面是 5/10/20/50/100(相邻约 2 倍, 且 100 保证最重那档每轮都被量到)
734	开跑时存下随机状态并种入固定种子
736	跑完把随机状态**原样还回去**(不还的话正常游戏每局球路一样, 比跑分不可比严重得多)
739	跑分时给球一条**按球号派生的碰撞随机流**(撞钉/墙/隔板/弧面全走 `b._rng`)
741	碰撞流的种子按**球号**派生(不是每发同一个, 否则 5 发走同一条轨迹)
744	跑分期间**不重掷盘面**(否则会把 `_auto_launch_tick` 钉好的覆盖掉)
746	装杯内部的下落时序也按倍率派生(球路钉死后 装杯 571/545 还差 26 帧, 就是它)
748	⚠️ 装杯那条**只对跑分生效**(`auto_close` 是跑分的标志) —— 正常游戏的杯子必须随机
753	历史面板表头是「中位跑分 / 波动」(那一格是 `_phys_sorted[len//2]`, **中位数**不是均值)
755	表头是「平均/1%Low帧」
761	「CPU平均频率」那一列已删(它已被实测证伪), 且表头仍走 `_HIST_COLS`
765	面板只剩三列、以「中位跑分 / 波动」收尾(`归一化` 只留在记录 JSON 里)
774	面板底部留着「中位跑分」的口径(它是**物理步数**的中位数, 不是渲染帧数)
776	「波动」写清了算式(玩家 2026-09-15 定稿的写法)(⚠️ 别看它没写 ×100 就以为和代码对不上: main.py:9600 是 `100.0 * (max-min)/fps`, 屏幕上印的是 `1.9%` —— 那个 %% 就是 ×100 的结果, 只是落在单位里)
801	底部口径**不解释**平均帧/1%Low帧(常识, 占了小半屏还折行 —— 玩家点名不要)
819	口径每项一行的宽度门禁跑不起来
821	日志头部印「低于中位帧率 55% 的帧数」+「内容配比」—— 判断这一轮和上一轮能不能比(门槛是**相对中位帧**的; 原来写死 90fps, 在 1%Low 贴到 89.7 时反而报'变差', 已改)
824	55%~75% 的参考带**单独一行**印出(玩家定稿: 只作参考, 不混进硬指标)
827	字号预热扩到 HUD 基准, 且基准值**从标签自己的 `_fit_base` 读**(不手抄 sp(N))
829	字号预热那一步用 0.02 秒(真机实测冷开一次约 1.4ms, 0.05 白白多花 3.6 秒启动)
831	缓存若被关掉, 日志**明说「本版关闭」**而不是印成 0% 命中率冒充失败
839	`settle()` 里的统计刷新**延后**了, 不在落袋当帧重建文字纹理
841	阴性对照: 裸的 `self._refresh_stats()` 已经不在 `settle()` 里了(留着就等于没挪)
844	延后用的是 0.20 秒而不是 0(0 = 下一帧, 仍在落袋窗口里)
846	两个压暗强度都在 (0,1]
889	两块压暗矩形都不与游戏区重叠(重叠 = 压暗叠成 0.90, 大字也会被压)
893	界面里除了游戏区**一块不漏**(含五行之间的间距与底部留白)
896	两块严丝合缝相接(中间不留缝, 也不重叠)
904	宽窗口下压暗铺满**视口**而不是 RootWidget(否则两侧留白原样亮着)
911	游戏区下方那两块(信息行/底行)在 game_area.y<0 时也要盖上(老写法算成高度 0)
919	视口尺寸一变, 压暗矩形跟着重算(bind 生效 —— 横竖屏切换/resize 全靠它)
925	两块 Color 都在 canvas.after 里(挂在 canvas 上会被五行盖住, 等于没压)
936	_sync_hud_dim 写成 DIM_RGB + HUD_ALPHA(与板面同一个色、同一个强度)
953	idle 时 dim_alpha() 归零(_settled_at 是上一局残值, 不判 idle 会算成永远全黑)
957	板面画过一帧之后, dim_alpha(peak) == peak(HUD 与板面同值、只有峰值不同)
964	演出结束那一帧 _redraw 已把 a_dim 归零(HUD 不会比板面多黑一帧)
1002	绘制次序 后层玻璃 < 压暗 < 前层玻璃(压暗画在后层玻璃之前 = 被玻璃提亮回去)
1015	压暗画在**弹珠之前**(放到球之后 = 球被压暗, 整堆沉进背景里看不见)
1060	每局演完 dim_alpha 必归零(卡住 = HUD 永久停在暗的)
1131	每一次落地音都对应恰好一次震动, 且时刻逐个相同(不自己另判一次节流)
1134	两次震动间隔不小于 BOUNCE_THROTTLE(否则会糊成持续嗡嗡)
1145	_build_info() 不抛异常(拿不到就返回空串)
1147	制作时刻格式 = 于 YYYY年MM月DD日 HH:MM 制作(年月日写汉字, 全数字容易读反)
1151	版本号**不再出现在正文行里**(玩家: 「去掉其他地方的版本号」) —— 正文只剩制作时刻
1155	弹窗标题 = **跳跳的弹珠机 v0.x.x**(游戏名与版本号之间**有一个空格** —— 玩家 2026-09-11 「加一个空格」); 版本号全工程只在这里出现一次
1162	时刻是 24 小时制(%H, 不是 %I): 23:05 要显示 23:05, 午夜要显示 00:05
1174	装杯落珠的音量不低于主游戏落槽(玩家报过'音量太小'; 主游戏是 clamp(vy/500, 0.3, 1.0))
1177	夹完不超 1.0(Sfx.play 的 lvl 上限就是原始 PCM, 超了等于没加)
1218	落袋后槽号/球心 x 不再变(落袋=终态)。这是让「结算槽==落格槽」与调用方循环形状无关的那条不变量; 少了它, 一帧 2 步就换槽
1222	落袋时球仍在下落(地板矩形不再被当弹性墙撞, 真实下落速度活到落袋那一刻)。以前 WALL_E=0.5 会在同一子步把球弹成向上, 实测 61% 的落袋 vy<0 —— 既让结算槽受偶然影响, 又把落地回弹抹掉四分之三
1233	落地回弹的最低 apex >= 10px(肉眼可见口径, 与碰钉弹高门禁同尺)。旧值 LAND_BOUNCE_MIN_VY=220 是把它当**回弹**速度写的, 实际只弹 ~4px, 74% 的落袋肉眼看不见
1241	落地高度带随机(用户定稿: 不要固定高度) —— 撞击速度乘随机系数, 落得越快弹得越高
1246	_frame 的飞行循环落袋即 break(少了它, 球落袋后还会被继续模拟, 结算时刻/槽位白闪/装杯/揭晓随帧率漂; 实测越槽局多飞 0.68~1.27s, 状态栏那段时间还打着「即将入袋…」)
1258	音效关掉时 audio_ready() 直接放行 —— 不这么做加载页永不摘 = 软锁(项目红线)
1261	audio_status() 在没有后端时也不抛异常(它只是隐藏菜单里的一行, 不许把弹窗带崩)
1269	audio_detail() 返回字段行(「X启动总耗时Nms(音效加载Mms)」/「无音频加载累计耗时：Nms」 / 加载进度 / 音频后端 —— 两支同一形状; 后端重建只在非 0 时出现, 「音效开关」已整段删除。v0.8.67 起「音效等待」一律叫「音效加载」; v0.8.69 起 PC 那支也补齐这一行、并按同一行序摆)
1284	「音效开关」那一行**已按玩家要求删除**(玩家原话:「去掉音效开关： xx 这个行 没有意义」) —— 它永远显示「已开」, 属于这块面板自己那条成文规则「有唯一预期值的, 只在偏离时才有信息」的反面
1302	后端不报数时显示「未知」,**绝不退回闸门值**报成 97 / 97 全绿(那个分支里四行有三行假绿, 只有「音频后端」说真话)
1311	「加载失败 …」整行拆掉, 语音那半成了「语音就绪：XX / YY」
1319	「语音就绪」紧跟在「音效就绪」后面(玩家: 「放在音效就绪的后面」; 2026-09-18 起两者并进同一行)
1332	彻底没有后端时「音频后端」那行印「静音」—— 和 PC 上正常态(winmm(8声道))不会长得一样(否则「全没了」和「本来就该这样」就分不出来了); 而「音效开关」那行已删除, 这条**替代**了原来靠它区分的那条断言
1352	PCM 后端(PC)下那三项**结构上不适用**的行直接不出现 —— 而不是印成三行「不适用」(玩家 2026-09-11: 「这几个不适用听起来有点奇怪」: 既是噪音, 又把有内容的两行淹了)
1362	PC 上也要报那几个时间, 且与安卓**同一形状**: 「X启动总耗时Nms(音效加载Mms)」 + 「无音频加载累计耗时：Nms」—— v0.8.69 起 PC 那支补齐(以前它缺那一行、名字还是老的「音效等待」、并且「音频后端」排在第一个)
1371	有实测值时「总耗时」读 `_total_ms`(摘页时钟) —— 不是 `bake_ms + ready_ms`(553) ; 玩家截图那个矛盾(463 < 1328)就是老算法漏掉「进程启动 -> 烘焙开工」那一段造成的
1377	取不到实测值(没建加载页 / 重放刚清过)时退回老算法, 而不是印 0
1382	音效开着且**还没就绪**时不放行(不然加载页形同虚设, 等于退回改动前)
1384	出货 main.py 里加载页的创建与摘除都按 audio_ready() 判(不是「烘完就摘」)
1386	sticky 广播首帧只记初值、不重建 —— 旧代码拿 0/1 去比 None 恒假, return 永不生效, 于是每次启动都白重建一次 SoundPool(冷路径下还会把 _ids 清在半路)
1389	后端提供 probe_all(能播探针) 与 loaded_count(后端真相, 诊断行要读它而不是闸门)
1394	「输出采样率」那一行连同它的取值口(_android_output_rate / _out_rate)已整段删除
1405	启动页**没有实时诊断行**: 那条路(_veil_status_line / set_status)已整段删除(那些数「启动信息」里全都有, 而且更全)
1408	启动页默认文案是**空的** —— 那五个大字是 `_build_title` 单独建的; 只有「重放冷启动」那条路会带文字进来(那是作者主动点开、专门停下来读的一屏)
1411	开场那行字**就是游戏名**(六个字 —— 玩家口述的"五个"不影响, 长短由 `len()` 自适应)
1424	启动页那条**图片路径已整段删除**(不再挂 presplash 那张图, 也没有盒子/夹断/占位图那些事)
1432	**t=0 那一帧**: 六个字还没淡入、填充线也还没起步(出来的时候才慢慢到齐, 不是一上来就满亮)
1443	整行**一起**淡入且**单调**, 最后到满(不回落)
1461	**KTV 那条填充线**: 一轮之内只增不减、这轮扫完到 1(整行都换过色)、下一轮从 0 重新扫
1464	圈次**只增不减**, 且**演完最后一种颜色回到第一种**(取模, 不是夹断) —— 玩家: 「无限演 …… 颜色1结束播放颜色2 颜色2结束播放颜色3 结束播放颜色1」
1470	色表是**一串 RGB**, 至少两种颜色; 顺序就是变色的顺序(银 -> 黄 -> 红 -> 银)
1475	那一行字**没有描边/阴影/辉光/缩放** —— 只有颜色(玩家: 「高级不是土味审美」「别tmd加奇怪的描边了 阴影了」)
1478	填充线的粒度是**像素级**的(两层字叠着 + 一条裁剪), 所以能切在某个字的中间 —— 玩家: 「圆点左边的字体是新颜色 甚至颗粒度可以很细什么的」
1489	**最短停留**够那行字**完整淡入**(否则字亮到一半就被摘 —— 半截动画比没有动画更像出 bug); **不要求**走完一整轮(那会逼加载快的机器白等一轮多)
1499	**前摇不短于整行淡入**(否则字会一边淡入一边变色 —— 玩家要的是「刚开始是都能看到的, 然后逐渐改变颜色」)
1505	一轮的时长 == 前摇+扫+后停(`_veil_title_round_len()` 与三个常量必须自洽)
1508	**最长停留 = 音效就绪的硬超时**(两个 6 秒同值): 一个管"别等太久", 一个管"最多演 6 秒",改一个必须看另一个
1511	**没有淡出** —— 到点整页直接消失(玩家 2026-09-11: 「这里应该是啥都看不见, 消失得干干净净, **而不是有淡出**」): 那行字一帧都不许压在游戏画面上
1514	历代两代动效(进场从小到大 v0.6.26~28 / 呼吸 v0.6.29~35)的常量与函数都已删除, **没有复活**
1531	**底色三处同值** = 没有突变的唯一保证: `VEIL_BG`(这一页) == `buildozer.spec` 的 `android.presplash_color`(系统那张图) == `p4a/hook.py` 注入的 `windowSplashScreenBackground`(应用启动窗口)
1552	`presplash.png` 还是那张**纯色图**(1080x1920 且体积很小) —— 系统那层和这一页之间才会是「同一个颜色接同一个颜色」
1557	presplash.png 可读
1559	启动页底色用 `VEIL_BG`(#0b1220), **不是**游戏的 `COL_BG` —— 两者不同是有意的:前者要和系统那两层对齐, 后者是游戏自己的背景
1562	_frame 每帧驱动启动页(它自己不持有 Clock: 切后台回来直接跳终态, 不会冻在半路); **就绪判据由 `tick()` 给** —— 它还要管开场动画的最短停留与整页淡出, 所以 `_frame` 不再自己看 `audio_ready()`
1578	关键方法都长在 RootWidget 上(不是掉进了隔壁 class —— 生成物里方法边界肉眼看不出来, 而掉错类**能编译**, 只有 hasattr 能问出'它长在哪')
1582	没有方法漏进 _LoadVeil(它只有一个 __init__ 和几个触摸/文案方法)
1618	拓扑合法: 每颗球发牌时, 它**正下方**(水平距 < 一个球直径且更低)的球都已发牌 —— 判据用水平距而不是 3D 距: 球是竖着落的, 水平距小于球直径就必然相交
1633	发牌顺序**每局不同**(不再是「12 个变体共享同一个顺序」): 逐位比对两局第 i 颗球的落点, 相同的比例必须很低(随机下期望约 1/n)。照旧实现下这里恒 = 1.000
1667	飞行球也按深度画(远的先画、近的后画): 发牌随机化之后飞行球不再恒为最近的球, 照旧把它一律置顶 = 更远的球盖住更近的已落定球
1723	长按入口**每次都弹**: `_rtp_unlocked`(只置真、从不置假, 一局只能解锁一次)已整个删除, 长按时长 5s -> 3s。留着它 = 第二个状态源(真状态在那排按钮里)
1761	不叠弹窗: 弹窗开着时再长按被闸门吞掉; 关掉之后能再弹(Window 级触摸观察者不受模态弹窗拦, 闸门必须开在建弹窗那一端)
1769	弹窗选项文字 == {关闭隐藏} ∪ RTP_HIDDEN 的档位文字 + 一个「确定」(手抄过一次档位文字, 隐藏档改定稿时漏改 -> 玩家 2026-09-11 截图报的就是这个)
1788	默认选中当前档位: 开着 2000% -> 高亮 2000%; 常驻档 -> 高亮「关闭隐藏」(恰好一个高亮)
1800	「确定」不能和选中态同色(同色时玩家分不清哪个是「当前选中」)
1813	关闭隐藏档: ① 按钮全部摘掉(那排回到 4 个常驻) ② 档位回到最高常驻档 ③ 布局 children 同步 ④ **本来就是常驻档时什么都不做**(不跳 360%)
1822	切到第二个隐藏档时**只留当前一个**(开过 1000% 又选 2000% 不该挂两个按钮)
1826	关闭隐藏档只播**一条**语音 voice_rtp_hide_2000, 且没有 voice_rtp_360(两条同走普通路径时 360 那句会抢掉 hide 那句(v0.8.65 起「打断旧的、念新的」), 所以 set_rtp 那边必须 silent=True)
1845	非 ready 时 set_rtp **完全不动**(档位高亮与盘面**两个都不许变**)
1852	【阴性对照】ready 时 set_rtp **照常生效**(证明上一条有分辨力)
1861	非 ready 时长按「期望返还比例」**不弹窗**(弹窗是 Window 子控件, 输入锁拦不住它)
1878	语音清单三方一致: generate_voice.PHRASES == voice/*.wav == 出货文件里播的字面名(双向; 少一个 wav = 真机上那条播不出来); 模板名(如 voice_rtp_hide_%d)另有前缀判据
1886	语音清单可读(generate_voice.PHRASES 与 voice/ 目录)
1893	隐藏档那套方法都长在 RootWidget 上(生成物里方法边界肉眼看不出来, 掉错类**能编译**、--check 也全绿, 只有 hasattr 问得出'它长在哪')
1946	装满后**不自动退场**: 干等 12 秒(既超过旧实现的 0.85s, 也超过 FX_MAX_SEC=9s), mode 仍是 result、_closing_at 仍是 0、busy() 仍锁着 —— 画面停在装满状态等玩家点击。改回「到点自动 idle」/ 给 result 段加时间兜底, 这里立刻红
1983	**跑分例外**: auto_close=True 时不点击也到点自己走(跑分是自动连续发射的, 等人点击会把整轮跑分卡死)
2022	_tex_coords_rot 是绕中心的纯旋转(角度可逆 + 保长)
2025	_tex_coords_rot(0) 等于 Rectangle 的默认 tex_coords(零旋转不改变外观)
2046	_update_slots 里调 lamps_off(增量重掷 = 整块重画, 少了它灯就永不熄灭)
2050	park_ball(reroll=True) 走的是增量 _update_slots(整块 _redraw 已被 0.6.91 换成它)
2077	夹具本身就位: 结算两局后恰好 2 格灯亮着
2082	换盘面后全部指示灯熄灭(玩家报的『点了不消失』)
2088	灭灯的同时槽色照常更新(没有为了灭灯牺牲增量语义)
2091	重掷后槽位白闪照常作废(_pulse 仍被清)
2112	成绩页取用了 groups 当分母(不取 = 又退回只印帧数, 那个数读不出结论)
2114	比例 = 该阶段进 1% 的帧数 / 该阶段总帧数(分母是 _tot, 不是 low1_n)
2140	成绩页印出了「卡顿帧分布」那一行
2144	分子与分母都原样印出(只印百分比 = 把样本量这条信息丢掉)
2149	按**比例**降序排(待机 4.2% > 装杯 3.0% > 飞行 0.3%) —— 第一个就是最该查的阶段
2156	缺 groups 时退化成只报帧数, 整块不消失(历史 diag 字典没有这个键)
2158	空 diag 仍返回空串(与原行为一致, 不让诊断把成绩挤没)
2172	曲线弹窗把逐帧阶段标签传给了 FpsCurve(不传 = 整个阶段条消失)
2175	_on_flip 每帧把场景标签记进 _bench_frames(阶段条与日志的唯一数据源)
2177	STAGE_ORDER 与 _bench_tag 都在(名字对不上会退化成灰色, 见常量处注释)
2182	标签长度对得上时 FpsCurve 收下它
2184	长度对不上时退化成不画阶段条(曲线照画, 绝不崩)
2186	压根没标签时也退化(老记录路径)
2230	日志头部带各阶段统计表(列头叫「长停顿率」, 与面板的「慢帧」区分开)
2232	头部给出『慢帧 × 文字重建』交叉表 —— 判断文字重排是不是元凶的直接读数
2235	头部报全程重建次数与落在慢帧上的次数
2237	头部带发声/震动单次最慢 —— 覆盖另一条假设(关音效会连震动一起关, 两条分不开)
2246	头部印出最慢三帧的 _frame 子步骤(发射那一刻的子步骤原来完全没有计时)
2249	launch 真的被 _brk_wrap 包上了(不包 = 那一格永远是空的)
2256	子步骤形状意外时退化成『无』, 整份日志不消失(实测踩到过 TypeError)
2260	表里的分母是该阶段的总帧数(与成绩页同一条口径)
2266	日志正文**每帧一行**, 一帧不少(静默丢帧 = 拿它分析会得出错的结论)
2269	正文格式是 `帧间隔,阶段,文字重建,_frame自算,主线程,发声,震动,最大子步骤,等屏幕`(新列**只能追加在末尾** —— 插在中间会让旧解析器静默错位)
2293	头部印出慢帧的 CPU 账(自算 / 主线程 中位)—— 分流「慢」是谁造成的
2296	主线程没在跑的慢帧被单独数出来(帧在等 ≠ 算不出来)
2299	主线程一直在算的慢帧也被单独数出来
2302	给了一把「常态帧占多少」的尺子(慢帧低于它 = 在等; 高于 = 真在算)
2304	正文每帧带上自算/主线程/最大子步骤三列
2308	`Label.texture_update` 自己也被 `_brk_add` 计时(子步骤里会出现「文字X.X」)
2312	头部印出「慢帧 × 发声/震动」的交叉表(与「慢帧 × 文字重建」同一个形状, 便于横向比)
2319	印出滞后窗口那一版(当帧视角会漏掉「发声后一两帧才卡」这种因果)
2322	正文每帧带上发声/震动两列(表头写明了)
2336	头部印出节拍真值(屏幕Hz / 请求的高刷模式 / Kivy上限 / vsync / multisamples) —— 跨日志比较的前提
2341	节拍那行的值取自诊断数据, 不是写死的占位
2344	正文表头写明最后一列是「等屏幕ms」
2356	头部给出「慢帧 × 等屏幕」交叉表 —— 那批主线程没烧 CPU 的慢帧到底在等谁, 看这一行
2359	头部给出等屏幕的全程中位 / 最慢 / 累计(占窗口百分比)
2365	等屏幕那一行的判据写明了**两个方向**(必须配合 CPU 账一起读, 不能只看 swap 高)
2372	头部印出 GC 次数/累计/单次最慢 + 实际生效的后端(降级到别的后端会改变所有音效读数的含义)
2375	头部印出进程态 utime/stime(内核占比高 = JNI/Binder/写盘那一族; 用户态高 = 纯 Python 抢 GIL)
2377	头部印出「最差 20 帧距上次发射多少帧」(发射后 0.33 秒内 = 发射本身贵)
2379	有 JNI 数据时印出主线程/后台两笔(主线程那笔才是卡我们的)
2381	阴性对照: 夹具里 cfg_n=0 时**不印**配置写盘那一行(不凭空造一行 0.0)
2393	头部印出**渲染窗口**那一段的 CPU 频率(最低/最高/上限 + 低频占比)—— 所有 CPU 数字的前提
2400	⚠️ 代表值印的是**平均**(1250) 而不是中位(1152); 中位降级到括号里, 与最低/最高并列成「分布的一项」
2403	写明了读法: 渲染窗口里应用大部分时间在等 vsync ⇒ 调频器判它很闲, **这一段天生偏低**, 要看跑分时跑到多少得另看那一行
2411	阴性对照: 没有 mean 时印「没采到」, **不许拿中位数冒充平均**
2413	阴性对照: 采不到频率(桌面/被 SELinux 挡住)**整段不印**, 不印一行 0 冒充量到了
2415	频率采样在开跑时启动、停采样时停掉(不留后台线程)
2420	频率采样**不在** `_on_flip` 里(埋点不许抬高它要解释的那个数)
2422	频率采样跑在工作线程上
2429	第二趟文字渲染(`CoreLabel._texture_fill`)也进了子步骤计时(日志里会出现「填纹X.X」)
2431	包的是 `LabelBase._texture_fill` 且**保住了 `__name__`**(Kivy 有按方法名找的地方, 改名会静默失联)
2434	取 `LabelBase` 走 try/except(换 Kivy 版本时不会把整个模块带崩)
2438	正文最后一列是该帧真实的等屏幕毫秒(不是恒 0 的占位)
2443	阴性对照: 一个 swap 值都没有时**不印**这一块(而不是印一行 0.00 冒充测到了)
2446	`_swap_wrap` 包的是 **类** 上的 `flip`(包实例属性撞不到 `self.flip()`)
2448	`Window.flip()` 的耗时真的被写进 `_FRAME_SWAP`
2450	埋点只在跑分采样期生效(平时不改变被测量的东西)
2452	`_on_flip` 读完就把 `_FRAME_SWAP` 归零(否则同一笔会被记进后面每一帧)
2454	`_start_benchmark` 里把 `_FRAME_SWAP` 归零(上一轮残值会变成第一帧的假读数)
2461	`_start_benchmark` 把发声/震动/预热三个逐帧计数也归零(第 0 行不再是跨窗口残值)
2463	`_on_flip` 里那三个「每帧清」仍然在(两处都要 —— 一处开跑清一次, 一处每帧清)
2480	`_texex_bake` 主动 bind 一次 —— 不 bind 的话预热一张纹理都没烘, 账全拖到运行期
2482	bind 排在 `refresh()` **之后**(refresh 之前连纹理都还没有)
2484	用 `bind()` 而不是读 `.pixels`(后者要把整张纹理拷进 Python 侧: 45KB/项 x 上百项)
2490	bind 有兜底 —— 它是要往 GL 里塞东西的, 失败绝不能把烘焙整条路带崩
2500	`_fit1` 记「叫了几次」
2502	计数排在重入闸**之后**(闸门里早退的不是活, 数进去会虚高)
2505	`text_px` 只数**缓存未命中**(= 真的现量了一回; 命中不算 —— 冷测量才是那笔钱)
2508	`_start_benchmark` 把分解计数归零(否则第一帧背着跨窗口残值, 印出个没人解释得了的数)
2511	`_on_flip` 把它存进 `_bench_frames` 的 [11] 并每帧归零
2518	★ 那行印「其中带字号 N」—— 用**分解计数**判, 不用子步骤名(那两帧的 `_fit1` 跑在别的回调里, 最大子步骤那栏未必写着「字号」)
2521	最慢三帧那行的「字号」带上 (次数/测量数)
2534	字号挑选走 `FIT_SCALES + FIT_FINE` 两段**固定阶梯**
2536	⚠️ 二分**已经删干净** —— 它在这个引擎里是结构性错误: 保证产出「这辈子只用一次」的字号, 而每个新字号都是一次 40 毫秒的冷字体表打开
2539	`FIT_HARD_FLOOR` 是 `FIT_FINE[-1]`(**只此一处真源**, 免得两处各写一个数漂掉)
2553	预热**只烘基础 6 档**(不烘 FIT_FINE) —— 烘满 13 档会超过字体缓存上限 64, 把前面烘好的全挤掉, 而且**不报错**
2564	⚠️ 预热**不许 round** 字号 —— 运行期用的是原值, round 过就是两个 fontid, 等于没烘
2566	大字/飘字**只烘第一档**(k=1.0) —— 一个都不烘会让 sp(48) 冷开 49 毫秒, 全烘又超上限; 第一档多半就放得下
2570	那 4 个基准与 `big_result_text` 里的 `sp(48)` / `sp(36)` 对齐(手抄的, 注释已标明)
2572	「最近预热档」那份清单**只收真的进了预热链的** —— 表里列着、链子却没碰它, 会印出两条自相矛盾的读法(实踩过)
2575	另记一份「预热**真的量过**哪些字号」(和上一条不同: 上一条是「表里列了什么」) —— 冷测量时两相对照才分得清「被挤掉」和「压根没烘」
2581	预热项数 ≤ 60(Kivy 字体缓存上限实测 64; 超了就是**预热自己把自己挤掉**)
2585	预热项数门禁跑不起来
2586	日志头部印预热项数 + 上限, 超了能一眼看出来(不然又得靠桌面探针回头查)
2595	字体缓存判据比的是**实际开过的 fontid 数**(`len(_FS_OPEN_SET)`), 不是预热表项数
2597	而且那个判据**排在预热项数那一行之前** —— 先看到的是真正会溢出缓存的那个数
2600	报告 UI(「帧率曲线」弹窗)自己的字号**不计入账本** —— 它是测量动作, 不是被测对象; 而跳过几次**必须印出来**(不许静默)
2603	两个**普查**计数器: 每个 fontid 冷开过几次(定钉几个)、本帧冷开几次的分布(回答「一帧会不会开十几个」—— `(N次/M测)` 是采样, 只有 2/21059 帧印过)
2606	两套 ★ 口径**各自带名印全** —— 它们都叫 ★ 却是两个量(实测第 2 轮 A=3 而 B=8, 差 167%; 而第 1 轮两者都给 1, 只看一轮永远发现不了)
2609	「尾」拆成两段(尾·回调 / 尾·空档)+ flip 自身 = 三段, 不再是一个 lump
2620	同类行: 度量 bold = 「组里只要有一个粗体就按粗体」, 且**度量与定档用同一个** `_bd`
2624	同类钮: 同上(逐个小 `_fit1` 会让长标签缩得更多, 所以这一排必须共用一个档)
2628	⚠️ 「最宽那一行自己的 bold」这个写法**已经彻底删掉** —— 它是预热烘不到的那个源头
2637	三个 Clock 回调都挂上了子步骤计时(自动发 / 起蓄 / 槽面)
2652	`尾` = 从 `_frame` 算完到真正交画面之间那一段(渲染 + 其余回调); 没有它分不出「主线程 − 自算」那 15 毫秒是渲染还是别的回调
2658	⚠️ `尾` 必须在**主路径那次** `_orig` 之前记 —— `_on_flip` 是在 `_orig` 里面被派发的, 它读完 `_FRAME_BRK` 就清空, 记晚了这一帧白记
2664	`_frame` 里把「这一刻」写进 `_FRAME_END`(全文件**只此一处** —— 两处会互相盖掉)
2672	⚠️ 那一句必须是 `_frame` 的**最后一行** —— 放中间的话 `尾` 会含住后面还没跑的部分, 读出来是假账(而且不报错)。判据: 它后面第一个非空行必须顶格(缩进 0 = 已出类)
2679	冷字号测量带 (毫秒, 字号, bold, 标签) 四元组, 且有低于门槛就不记的下限
2682	日志头部两个分支都印(有冷字号就排名, 没有就明说「一次都没有」 —— 别让「没印」和「没发生」混在一起)
2684	`_start_benchmark` 清空冷字号榜(不清的话第一份日志里混着上一轮的残值)
2686	`_fit1` 记下「谁在挑字号」(给冷字号归因)
2693	冷字号测量记下**基准字号**(倍率 = fs/基准, 用来区分 FIT_SCALES 与 FIT_FINE)
2695	日志印出「基准 / 倍率」两栏
2697	日志里写明读法2: 倍率落在 FIT_SCALES 之外 ⇒ 那是**硬取舍**, 不是漏烘
2715	`_redraw` 仍分「杯层 / 球层」两段计时(持久指令表把重建摊平之后, 这两格就是读每帧真花了多少的唯一窗口)
2717	先取时再记账, 且杯层在球层之前(次序错了会量成负数或重叠)
2725	持久指令表四个函数都在
2728	⚠️ 球层按 `self._balls` **原始顺序单趟建**(红线 1) —— 历史账: 改成「落定球 + 飞行球两趟」后 x100 一局 25.5% 的重叠帧层次错乱
2733	⚠️ 玻璃五层顺序不许动(红线 2): 阴影 < 后层 < 压暗 < 杯口环补画 < 弹珠 < 前层
2735	不可见的球只把 size 置 0、**不增删指令** —— 条数在本局内必须恒定
2737	⚠️ 重建判据里球列表比**对象身份** —— 新一局长度可能一样(8 换 8), 只比长度会拿上一局的球继续画
2741	⚠️ `idle` 与「两边都透明」两种早退都要**显式作废表**(红线 3) —— 少了它退出装杯后杯子/压暗会永久留在画布上(canvas.clear 是唯一能擦掉它们的机制)
2744	每帧存**前四**个子步骤(不是前二): `杯层/球层` 嵌在 `装杯` 里、`装杯` 又嵌在 `板面` 里, 只存前二时它们**永远进不了榜**(实测: 拆完一个都看不到)
2747	「发·整段」排在各分段**之后**(它是包住全体的总和, 放前面就只量到一半)
2768	表头与数据行**共用同一个字号**(`_fs_all`, 逐列量完取最小), 基准 sp(14) (2026-09-14 玩家:「这个时间 平均/1%low帧什么的标题字体的大小增加」; 2026-09-15 玩家:「项目内的字体大小改成相同」—— **别再退回逐列各缩**, 那会让表头比数据行小一档)
2774	⚠️ 表头**不再**把 `text_size` 两维都绑成 size —— 那正是折行的直接原因
2779	**不再用空格分隔**(玩家 2026-09-15 定案) —— `_HIST_SEP` 应当已被整个删掉
2781	表头与数据行**共用同一套列宽**(`_HW`)与同一份列名(`_HIST_COLS`)
2783	数据行是**一排固定列宽的 Label**(不是整行一个 Label) —— 这才叫准表格
2785	行是**单行 Label**, `text_size` 只绑宽度(绑成 `w.size` 就会折行 —— 表头就是这么折的)
2787	频率那一格**整段删掉了**(连 `freq_text` 都不该再有); ⚠️ `phys_freq_mean` 仍存在 JSON 里, 只是不再显示
2798	脚注那段文字里**没有 markdown 星号**(Kivy 不认, 会在屏幕上原样显示两个星号)
2800	⚠️ 开头那个「口径：」**已经删掉**(玩家定稿: 那两行本身就是注释, 前面再挂标签是废话)
2814	**标题**用金色 COL_BALL（2026-09-15 玩家:「标题用金色」）
2818	**那段说明**用灰色 COL_SUB（2026-09-15 玩家:「备注用灰色」; 与 09-14 那版对调）
2821	口径标签走 `_auto_h`(高度跟真实排版走) —— 写死高度会让折行后的最后一行被裁掉
2827	⚠️ **不许**对历史面板调 `_popup_fit_content` —— 那会把空态弹窗收小, 而这个面板以后要装数据, 高度必须恒定(玩家当场纠正过)
2832	成绩面板 `auto_dismiss=False` —— 玩家定稿「这个界面只能通过关闭按钮关闭」(原来根本没有关闭按钮, 点面板外面就把它关了)
2835	「帧率曲线」和「关闭」在**同一行**(横向 BoxLayout; 先 add 的在左边)
2837	两个按钮各自接对了回调
2840	⚠️ 按钮那行**必须最后 add** —— 竖向 BoxLayout 按 add 先后自上而下排, 第一版加在成绩块后面, 结果两个按钮卡在面板**中间**(截图为证)
2850	⚠️ 成绩面板底部的诊断块**只留玩家看得懂的**: 删掉「1% Low 定位」(教学)、「主线程/游戏逻辑」(开发者术语)、「文字纹理 N 次」(纯埋点) —— 玩家 2026-09-15 点检过
2856	面板底部留着「最慢一帧：N 毫秒（阶段）」与**两条**分布(卡顿帧 / 慢帧)
2870	两档门槛比例**只在一处定义**(JANK_RATE=0.55 / SLOW_RATE=0.75)
2872	日志头两颗 ★ 的门槛从**常量**派生, 不是裸数字
2874	⚠️ 绝对帧率判据**已经删干净**(它在高刷机上没有意义, 且交接文档口径是相对的)
2876	日志印两行: 硬指标(中位帧率 55%) + 参考带(55%~75%), 且参考带那行**明说它不是指标**
2882	采样期每 0.5 秒记一次「阶段 + 屏幕刷新率」
2884	那个 tick 是 0.5 秒一次 —— `_screen_hz()` 在安卓上走 JNI, **绝不能每帧调**
2886	采样一结束 tick 就**自己摘掉**(返回 False), 不然跑完分还在后台戳 JNI
2888	日志印这一行; **采不到也明说** —— 本工程反复踩过「没印」被读成「没发生」
2890	参考带 = (中位帧间隔/SLOW_RATE, 中位帧间隔/JANK_RATE] 这一段
2897	面板印**两档**(玩家定稿的名字: 「改名字即可」): 卡顿帧（<55%）与 慢帧（<75%）
2899	⚠️ 不变量: 慢帧(75%) 一定 ⊇ 卡顿帧(55%) —— 老 diag 字典没有 slow_n 时直接印 0 会出现「卡顿帧 6 / 慢帧 0」这种自相矛盾的两行(探针夹具上撞到过)
2905	面板加「采样窗口：X 秒 · N 帧 · 中位 N 帧/秒」(数据来自 diag 的 win_ms)
2907	⚠️ 窗口时长 = **帧间隔之和**, 与日志头 `sum(gaps)` 同一口径 —— 别用 `_flip_times` 首尾相减(那个含首帧之前一段, 会比日志多一帧)
2910	面板印的门槛是**算出来的**(中位 x 比例 -> 帧/秒), 不是写死的
2915	⚠️ 慢帧的**帧数与分布取自同一个 list**(不许两趟各算一遍)
2919	日志印「面板窗口 vs 日志窗口」自证行, 不一致要显式喊出来
2921	⚠️ 面板上的「低于 60 FPS 的帧」已经删掉 —— 绝对门槛, 而且它和卡顿帧不是同一批帧
2923	分布那一行的口径统一到卡顿帧(原来用的是最慢 1%, 两行会对不上)
2928	慢帧分布读的是 `slow_groups`(<75% 那一档), 不是 `slow2_*` 那批
2931	⚠️ 该档为 0 时不许退回 low1_groups —— 否则印着分布却写着共 0 帧(守卫现在在共用的 `_dist_line_of` 里, 两条分布一起受管)
2935	空态用上下两根 `size_hint_y=1` 的弹簧把内容**竖向居中**(不是改高度)
2937	弹簧只加在**空态**分支里 —— 有记录时那块是 size_hint=(1,1) 的 ScrollView, 它自己会吃掉剩余空间, 再加弹簧反而挤掉列表
2951	被截断的「重建来源」补上其余N类与合计 —— 76 次与四项相加 68 不再自相矛盾
2954	阴性对照: 没有截断(合计==列出的和)时**不印**「其余」那一段, 不凭空造一行
2956	采集端把「前 4 名 + 其余合计 + 标签总数」一起存下来(不是只改打印端)
2965	复制走真剪贴板且往返逐字一致(复制按钮的全部意义就在这条路径上)
2968	复制路径抛异常
2971	复制失败会把原因写在按钮上(不静默失败 —— 静默失败等于让人白等一次出包)
2982	保存出来的 txt 与内存里那份**逐字一致**(少一个字节都算白跑)
2990	保存路径抛异常
2993	安卓端走 MediaStore 写**公共 Download 目录**(写 user_data_dir 的话文件管理器看不见 = 等于没存)
2995	写流时用 `java.lang.String.getBytes` 拿到真 byte[] —— 直接给 Python bytes 可能被 pyjnius 挑中 `write(int)` 重载, 于是只写进去一个字节(静默截断成一个字符)
2998	逐级降级全部失败后退回剪贴板(复用已测过的那个方法, 不另写一份)
3018	_set_controls_enabled 里**一处 `.disabled` 赋值都不许有**(每个 = 一次文字纹理重排)
3021	它改成写输入锁标志
3027	整个出货文件里没有一处 `.disabled =` 赋值(否则贵的写法又回来了)
3039	锁着时 on_touch_down 直接吞掉(在碰 touch 之前就 return True)
3046	解锁时它**走正常派发**(拿 None 当 touch 会抛 —— 说明没有把所有人都吞掉)
3067	有 `_restyle_buttons`(按钮底色的唯一取值口)
3069	旧的 `_restyle_selects` 已整个消失(它不感知输入锁, 禁用期会把选中项刷亮)
3071	`dim_rgb` 定义在配色块**之后** —— `keep`/`base` 是默认参数, 在 `def` 那一刻求值, 放前面 = import 期 NameError(同 `_TINT_BRIGHT` 的坑)
3082	底色只在 `_restyle_buttons` 里写 —— 这 5 个函数体里一处 `background_color` 赋值都没有
3090	`_frame` 里的 `background_color` 赋值**仍然只有 1 处**(蓄力期发射键的力度色)
3093	`_frame` **不许**调 `_restyle_buttons`(12 次赋值/帧, 还会跟力度色抢帧)
3101	序不许反转: 窗口底色 <= 不可用(未选中) <= 不可用(选中), 逐通道成立
3105	不可用的未选中档仍**亮于窗口底色** Δmax>=15(按钮不许被压到背景以下)
3109	不可用时「选中 vs 未选中」仍分得开(玩家要的: 看得出我押的是哪一档)
3117	阴性对照: 逐通道乘系数(old 写法)的可见度**确实更差** —— 判据有分辨力
3147	每颗球各一对 PushMatrix/PopMatrix + 一条 Rotate(丢一对 = 该球的旋转叠到后面所有指令上)
3161	矩阵栈不倒挂、且结束时归零
3167	每条 Rotate 都紧跟在一条 PushMatrix 之后(而不是被漏在外面)
3178	夹具定位不到前层玻璃矩形(门禁自身失效, 不是通过)
3180	前层玻璃画在矩阵栈归零之后(否则杯子被球层残留的旋转拧走)
3192	`glass_tumbler_over.png` 能加载且是 1600x920
3208	表里有 `oseam` 这一条 —— 摘掉它 = 接缝回来
3213	`oseam` 排在 **压暗之后、补画之前**(次序红线不动)
3220	有 over 时 **杯口那 12 段补画让位**(只留杯底那段), 不会把环画两遍
3230	阴性对照: 关掉 over ⇒ oseam 消失、补画回到 24 段(判据有分辨力)
